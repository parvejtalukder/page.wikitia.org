// import React from 'react';

const Mobile = ({url}) => {
    return (
        <div className="bg-white flex justify-around items-center border-t list-none">
            {url}
        </div>
    );
};

export default Mobile;